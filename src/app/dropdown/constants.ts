import { DropdownTarget } from './dropdown.models';

export const DROPDOWN_MARGIN_PX = 8;

export const DROPDOWN_INTERSECTION_THRESHOLD = 0.5;

export const DROPDOWN_DEBOUNCE_TIME = 300;

export const DEFAULT_TARGET_DATA: DropdownTarget = {
  targetTemplate: null,
  targetContext: null,
  targetElement: null,
  position: null,
};
